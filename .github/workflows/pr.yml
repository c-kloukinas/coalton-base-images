name: Build base images

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  build-images:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        LISP_IMPLEMENTATION: ["sbcl", "ccl"]

    env:
      ALLEGRO_LICENSE: ${{ secrets.ALLEGRO_LICENSE }}

    steps:
      - uses: actions/checkout@v4
      - name: Build the Docker image
        run: |
               docker build ${{ matrix.LISP_IMPLEMENTATION }} --build-arg ALLEGRO_LICENSE --tag ghcr.io/${{ github.repository_owner }}/${{ matrix.LISP_IMPLEMENTATION }}-base:latest
